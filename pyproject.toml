[project]
name = "rest-at-home"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "akil", email = "akilmarshall@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pandas>=3.0.5",
    "requests>=2.34.2",
]

[project.scripts]
rest-at-home = "rest_at_home:main"

update-at-home = "rest_at_home:update"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
